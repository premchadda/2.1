diff --git a/apps/backend/src/api/routes/admin-sections.js b/apps/backend/src/api/routes/admin-sections.js
index 86d54f6..a0f86ef 100644
--- a/apps/backend/src/api/routes/admin-sections.js
+++ b/apps/backend/src/api/routes/admin-sections.js
@@ -850,7 +850,7 @@ router.post("/seed-templates", protect, admin, async (req, res) => {
         expected_questions: 100,
         total_marks: 100,
         marks_per_question: 1,
-        negative_marks: 0.25,
+        negative_marks: 0.5,
         time_limit: 7200,
       },
       {
diff --git a/apps/backend/src/modules/tests/test.routes.js b/apps/backend/src/modules/tests/test.routes.js
index bbd3d36..c2a6d32 100644
--- a/apps/backend/src/modules/tests/test.routes.js
+++ b/apps/backend/src/modules/tests/test.routes.js
@@ -438,16 +438,33 @@ const getCorrectOption = (question) => {
   );
 };
 
+const answerIndexFallbackAllowed = (answers) => {
+  // Only fall back to index when no answer carries a questionId at all
+  // (legacy payloads). Mixed id+index matching is unsafe after reordering.
+  return answers.every(
+    (a) =>
+      a?.questionId === undefined ||
+      a?.questionId === null ||
+      a?.questionId === "",
+  );
+};
+
 const getUserAnswerForQuestion = (attempt, question, index) => {
   const answers = Array.isArray(attempt?.answers) ? attempt.answers : [];
   const questionId = getQuestionId(question);
 
-  const found = answers.find(
-    (answer) =>
-      idsMatch(answer?.questionId, questionId) ||
-      (answer?.questionIndex !== undefined &&
-        Number(answer.questionIndex) === index),
+  // Prefer stable questionId only. Index fallback caused cross-section
+  // answer attribution when review/result order differed from submit order.
+  let found = answers.find((answer) =>
+    idsMatch(answer?.questionId, questionId),
   );
+  if (!found && questionId == null && answerIndexFallbackAllowed(answers)) {
+    found = answers.find(
+      (answer) =>
+        answer?.questionIndex !== undefined &&
+        Number(answer.questionIndex) === index,
+    );
+  }
 
   return found?.selectedOption ?? null;
 };
@@ -601,12 +618,21 @@ const buildResultPayload = (
   const userAnswers = questions.map((q, idx) => {
     const answers = Array.isArray(attempt?.answers) ? attempt.answers : [];
     const qid = q.id || q._id;
-    const found = answers.find(
-      (answer) =>
-        (answer?.questionId && String(answer.questionId) === String(qid)) ||
-        (answer?.questionIndex !== undefined &&
-          Number(answer.questionIndex) === idx),
+    // Match by questionId only to avoid cross-section mis-attribution after reorder
+    let found = answers.find(
+      (answer) => answer?.questionId && String(answer.questionId) === String(qid),
     );
+    if (
+      !found &&
+      answerIndexFallbackAllowed(answers) &&
+      (qid === undefined || qid === null)
+    ) {
+      found = answers.find(
+        (answer) =>
+          answer?.questionIndex !== undefined &&
+          Number(answer.questionIndex) === idx,
+      );
+    }
     return {
       selectedOption:
         found &&
@@ -704,6 +730,7 @@ const buildResultPayload = (
     predictedRank: rankData?.predictedRank || null,
     isCalibrated: Boolean(rankData?.isCalibrated),
     sectionTimers: attempt?.sectionTimers || attempt?.section_timers || {},
+    sectionScores: attempt?.sectionScores || attempt?.section_scores || {},
     currentSection: attempt?.currentSection || null,
     questions: questionsWithAnswers,
     userAnswers,
diff --git a/apps/frontend/src/pages/tests/TestInterface.jsx b/apps/frontend/src/pages/tests/TestInterface.jsx
index PLACEHOLDER..PLACEHOLDER 100644
--- a/apps/frontend/src/pages/tests/TestInterface.jsx
+++ b/apps/frontend/src/pages/tests/TestInterface.jsx
@@ -1060,7 +1060,8 @@
       isPaused ||
       showPauseModal ||
-      showSubmitSummary
+      showSubmitSummary ||
+      isSubmitting
     )
       return;
@@ -1078,7 +1079,7 @@
     return () => clearInterval(interval);
-  }, [reviewMode, loading, test, isPaused, showPauseModal, showSubmitSummary]);
+  }, [reviewMode, loading, test, isPaused, showPauseModal, showSubmitSummary, isSubmitting]);
 
@@ -1990,6 +1991,14 @@
     setIsSubmitting(true);
+    // Freeze clock + section timers at click time so UI and saved stats match
+    // even if the network submit takes longer.
+    const frozenTimeSpent = Math.max(
+      0,
+      (test?.duration || 60) * 60 - timeLeft,
+    );
+    const frozenSectionTimers = computeSectionTimers();
     try {
@@ -2005,6 +2014,7 @@
             questionId: question.id || question._id,
             questionIndex: index,
             selectedOption,
+            timeSpent: Number(questionTimers[index] || 0),
           };
@@ -2025,10 +2035,10 @@
         {
           attemptId,
-          timeSpent: (test?.duration || 60) * 60 - timeLeft,
+          timeSpent: frozenTimeSpent,
           answers: submittedAnswers,
           markedForReview: Array.from(markedForReview),
-          sectionTimers: computeSectionTimers(),
+          sectionTimers: frozenSectionTimers,
           currentSection,
           disableNegativeMarking,
         },
