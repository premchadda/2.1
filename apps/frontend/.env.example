# Frontend Environment Variables (Example)
# Copy this file to .env and fill in your values

# API Base URL
# - Development: Use /api (Vite proxy handles routing to backend on port 5001)
# - Production: Set to your actual backend URL (e.g., https://api.yourdomain.com)
VITE_API_URL=/api

# WebSocket URL
# - Development: Use / (Vite proxy handles routing)
# - Production: Set to your actual WebSocket URL (e.g., https://api.yourdomain.com)
VITE_SOCKET_URL=/

# Admin Panel URL
# - Development: Use /admin (Vite proxy handles routing)
# - Production: Set to your actual admin panel URL
VITE_ADMIN_URL=/admin