// Re-export from consolidated dataService for backward compatibility
export { 
  authAPI, 
  seriesAPI, 
  testsAPI, 
  userAPI, 
  studyAPI,
  questionsAPI,
  examAPI,
  fetchFromAPI,
  apiClient,
  apiClient as default 
} from './dataService'
