// Canonical source: @trstprep/shared-hooks.
// NOTE: bare specifier on purpose — vite resolve.alias bypasses the
// package.json "exports" map for subpath imports (see Logo.jsx note).
export { EmptyState } from "@trstprep/shared-hooks";
